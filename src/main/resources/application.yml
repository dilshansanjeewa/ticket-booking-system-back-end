spring:
  datasource:
    url: jdbc:mysql://localhost:3306/ticket_booking_db?createDatabaseIfNotExist=true
    username: root
    password: admin

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true